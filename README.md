# xdr_test

```shell
# build docker
# docker build -t kali_linux_docker_secvision . 

# run docker
# docker run -d --network=host --restart always -e "CODE=I2R3-ZPA0-A53A" -e "SERVER_HOST=100000.secvision.cloud" kali_linux_docker_secvision:latest

```
